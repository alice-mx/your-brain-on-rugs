export default class {
  constructor(x1, y1, x2, y2, startColor, stopColor) {
    this.x1 = x1;
    this.y1 = y1;
    this.x2 = x2;
    this.y2 = y2;
    this.startColor = startColor;
    this.stopColor = stopColor;
  }
}
